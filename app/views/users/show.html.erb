<h4 align="center"><%= @user.username %>'s worktimes</h4>
<div class="center">
  <table>
    <tr><td>begin</td><td>end</td></tr>
    <%  @user.worktimes.each do |worktime| %>
    <tr><td><%= worktime.ontime %></td><td><%= worktime.offtime %></td></tr>
  <% end %>
  </table>
</div>
